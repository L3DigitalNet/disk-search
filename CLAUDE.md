# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Public repository.** Do not put secrets, internal hostnames/IPs, or infrastructure addresses in this file, in docs, or in commits. Runtime secrets are resolved from OpenBao (paths are referenced in the spec); a local `.env` is for development only.

## Project status: design phase — no application code yet

This repo currently contains **only planning and research documents**. There is no build system, no test suite, and no runnable code. The tool is fully specified but not yet implemented. **Do not invent build/lint/test commands** — none exist. When implementation starts, the intended toolchain (below) determines them, and this section should be replaced with the real commands.

## What this project is

A search-and-monitoring tool that watches ~20 online marketplaces (manufacturer recert stores, storage-specialist resellers, eBay/Amazon/Newegg, refurb server sellers) for HDDs and SSDs, and scores each listing to surface the best deals for a homelab/small-business buyer who favors **enterprise/NAS-grade** and **recertified** drives. It alerts on availability and price drops. Personal/business use; single maintainer.

## Key documents — read these to understand the system

| Doc | Role |
| --- | --- |
| [`docs/specs/disk-search.md`](docs/specs/disk-search.md) | **The spec — source of truth.** Read first. Features, marketplace tiering, scoring factors, stack, deployment. Uses `_TBD_` markers for undecided decisions. |
| [`docs/gap-analysis.md`](docs/gap-analysis.md) | Operational/engineering gaps in the spec **not** covered by research, each with a research-backed proposed solution (auth, secrets, deploy, backups, observability, currency, scoring edges). |
| [`docs/further-research-needed-prompts.md`](docs/further-research-needed-prompts.md) | 12 queued ChatGPT Deep Research prompts for undecided **domain** areas (drive grading, scoring math, legal/ToS, entity resolution, …). Each maps to a spec gap. |
| [`docs/research/`](docs/research/) | Completed research reports (dated, frontmatter'd markdown). |
| [`docs/research/index.md`](docs/research/index.md) | **Generated** index of the research reports — do **not** hand-edit; it is regenerated by the research tooling. |

## Planned architecture (the big picture)

The design is spread across the spec and the research reports; this is the synthesis worth knowing before touching any of it.

- **Acquisition is tiered by source, not uniform.** Prefer official APIs (eBay Browse/Feed, Amazon SP-API where authorized, Newegg is seller-side only) → machine-readable structured data (JSON-LD / hidden JSON) → headless scrape → skip. The ~20 marketplaces are **ranked by trust posture** in the spec; the recert specialists (WD/Seagate recert stores, ServerPartDeals, goHardDrive) are the primary value targets. Search APIs (Serper/Brave/Tavily) are for **discovery**, not authoritative state.
- **Pipeline stages:** `fetch → parse → normalize → entity-resolve → score → persist → alert`. Stages should be independently testable and re-runnable.
- **Canonical-entity model (critical):** the canonical entity is the **drive model** (stable identity — MPN/GTIN, capacity, interface, tier), and each retail page is a **listing/offer** representation of it. Repeated price checks are **time-series observations**. Do **not** model a retail page as "the product" — the hard problem is recognizing the same physical drive across merchants with different titles/SKUs while keeping condition (new vs recertified) and variants (capacity/interface) distinct. See [`docs/research/database-architecture.md`](docs/research/database-architecture.md).
- **Scoring:** a 0–100 composite from price (**USD per TB**, ideally relative to a moving baseline), availability, cross-marketplace-normalized seller reputation, and fitness-for-purpose (enterprise/NAS grade, warranty, condition). Scores should stay **explainable** (a listing can show _why_ it scored what it did). The scoring math itself is still to be designed (research prompt #4).
- **Intended stack** (from the spec; some still `_TBD_`): **Python**; **Scrapy** for scraping; **PostgreSQL** (research recommends **+ TimescaleDB** for the price-history/observation side); web framework **FastAPI or Django** (undecided); **uv** for env/deps; **NGINX + Let's Encrypt**; deployed to a single **Debian 13 VM**; **GitHub Actions** CD; email alerts via **AgentMail**. When you resolve a `_TBD_`, update the spec.

## Conventions & workflow

- **Resolving decisions:** the spec's `_TBD_` markers and the gap analysis are the open-question backlog. Domain questions are answered by running the queued Deep Research prompts; operational questions are answered in `gap-analysis.md`, backed by reports under `docs/research/`. When new research lands, **reconcile findings back into the spec** rather than leaving them only in the report.
- **Cross-document links are relative.** The spec lives at `docs/specs/disk-search.md`; docs elsewhere link to it as `specs/disk-search.md` (from within `docs/`). Keep links working when moving files.
- **Time-sensitive facts** in research reports (US import de-minimis / tariff status, tool CVEs, library maintenance status) are **dated** — re-verify before relying on them; several were in active flux as of the report dates.
- **Git:** solo-developer repo — commit directly to branches (main = stable, development = ongoing, feature branches as needed). No PRs unless collaborating. Conventional commit messages.
